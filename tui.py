#using textual for tui 
# must include boxes for each running agent( max 8) that display their reasoning/ thinking and output live
# a small indicator on each box saying name/prompt time alive total token and cost(color coded) 
