# runs and names each claude code instance so that they could be identified and monitored later
import pm2
class Runner:
    def __init__():

    def set_env_wizard(self):
        #Prompts user to set claude code api key and sets env if not found in .env file, else it uses key 
        pass

    def get_config(self, name: str, command: str,env: dict ):
        #generates and gets get_config

    def run(prompt: str):
        # runs claude code in -p mode and in background if set 
        pass 
